import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.teuliex.app',
  appName: 'Teuliex',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
